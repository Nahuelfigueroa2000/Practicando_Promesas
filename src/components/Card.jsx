export default function Cards({title,id,body}){
    return(
        <div>
        <h1>new title,{title}</h1>
        <h2>is it id, {id}</h2>
        <h3>you say body,{body}</h3>
        </div>
    )
}

